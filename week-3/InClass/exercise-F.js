// In pairs, write a function that checks a username is of an acceptable format for a user type. The function must:

// take two parameters: one for the username and one for the user type
// if the username starts with a capital letter and has length between 5 and 10 characters long, it must return "Username valid"; otherwise, it must return "Username invalid"
// if the user type is an admin or a manager, all usernames must return "Username valid"In pairs, 
// et numero01=5;
// function validarNumero(numero01){
//     if(numero01>3 && numero01<10 ){
//         console.log( true);
//     }else{
//         console.log(false);
//     }
// }
// validarNumero(numero01);

var nombre = Zaquiel;

function username(a, b){
    if(nombre >= nombre.length(5) && nombre <= nombre.length(10))
    return console.log(username(Username valid));
}
else{
    console.log(username("username invalido"));
}


let username = "Zaquiel";
let typofUsername = "admin";

function validacion(username, typofUsername)

if (username.toUpperCase == username) && (username.length >= 5) && (username.length<= 10)) {
    return "username valid";

}
console.log(validacion("admin", ))